<p id="notice"><%= notice %></p>

<h1>Cars</h1>

<table>
  <thead>
    <tr>
      <th>Model</th>
      <th>Vin</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cars.each do |car| %>
      <tr>
        <td><%= car.model %></td>
        <td><%= car.vin %></td>
        <td><%= link_to 'Show', car %></td>
        <td><%= link_to 'Edit', edit_car_path(car) %></td>
        <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Car', new_car_path %>

<%= form_tag(search_cars_url, method: "get") do%>
  <%= label_tag(:query, "Search for car(by model):")%>
  <%= text_field_tag(:query)%>
  <%= submit_tag("Search")%>
<% end %>

<%= link_to 'Parts', 'parts#index'%>

<%= link_to 'Cars', 'cars#index'%>
